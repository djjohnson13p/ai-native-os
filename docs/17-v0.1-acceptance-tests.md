# 17 — v0.1 Acceptance and Test Plan

The v0.1 prototype is successful only if the architecture can be demonstrated and tested. A visually impressive assistant that bypasses the contracts below does not satisfy v0.1.

## Test environment

Initial reference environment:

- x86_64 Linux host;
- one deterministic local provider set for file/table/chart/report operations;
- one local model adapter;
- one optional remote model adapter;
- container/sandbox support;
- SQLite or equivalent embedded state/provenance store;
- Windows compatibility habitat for Demonstration B;
- synthetic/non-sensitive test data.

Exact distro, model, UI toolkit, and IPC mechanism are intentionally not release requirements yet.

## Deterministic trusted-spine prerequisite

Before model/planner orchestration is allowed to become the primary implementation focus, the project MUST pass the Stage-1 deterministic gate in:

- `docs/86-stage1-deterministic-spine-acceptance-tests.md`

That gate proves Task state, Artifact publication, provenance, semantic/provider registry, authority/grants, credential mediation, immutable Execution Bindings, one deterministic Provider Supervisor path, and crash recovery **without requiring an AI model**.

The associated implementation-sensitive requirements are in:

- `docs/85-v0.1-trusted-core-requirements-addendum.md`

Passing the Stage-1 gate does not complete v0.1; it establishes the trustworthy substrate on which the AI/planner portions of this acceptance plan run.

## AIOS IR acceptance gate

The semantic IR boundary must work independently of model quality before it is permitted to drive real capability execution.

### IR1 — Demonstration A representation

`examples/aios-ir/demonstration-a.ir.json` must represent the full flagship graph without provider IDs, host executable paths, authority grants, secret values, or hardware-specific process details.

Pass criteria:

- validates structurally against `specs/aios-ir.schema.json`;
- every node resolves to a semantic capability contract;
- every input/output port type is compatible;
- all graph references resolve;
- graph is acyclic;
- required authority lower bounds are present and requested authority remains within referenced capability-contract upper bounds;
- egress declarations are compatible with capability contracts and internally consistent;
- policy-controlled egress has matching semantic `data.egress` authority/destination class where required;
- cache policy is compatible with execution class.

### IR2 — Positive and negative fixtures

Run every program in:

- `examples/aios-ir/valid-cases.json`;
- `examples/aios-ir/invalid-cases.json`;
- `examples/aios-ir/invalid-semantic-cases.json`.

Pass criteria:

- all valid fixtures are accepted;
- all invalid fixtures are rejected;
- invalid fixtures report the expected stable reason-code family;
- validator never invokes a model to decide whether malformed IR is safe.

### IR3 — Semantic/runtime separation

Inject provider IDs, capability-token/grant values, and transient runtime identifiers into semantic nodes.

Pass criteria:

- semantic schema/validator rejects them;
- equivalent information is representable only through a separate Execution Binding after policy/provider selection;
- changing provider binding does not change the semantic program hash.

### IR4 — Canonical identity

Take one valid semantic program and change only JSON whitespace/object-key ordering or explicitly non-semantic authoring metadata.

Pass criteria:

- normalized semantic hash remains identical;
- changing a semantic capability, authority action/resource selector, egress mode/destination class, type, verification gate/data path, constraint, cache policy, or failure behavior changes the semantic hash as specified;
- Task ID/provider/binding/grant/path/endpoint changes cannot enter the semantic hash because they are not AIOS IR semantic fields.

### IR5 — Graph/type failures

Inject:

- dangling node reference;
- missing output port;
- duplicate node ID;
- data-flow cycle;
- incompatible semantic input type;
- unknown capability.

Pass criteria:

- every case fails closed before provider execution;
- failure identifies the relevant node/port/capability where possible.

### IR6 — Bounded execution behavior

Inject an unbounded retry/replan request and recursive/broadening fallback.

Pass criteria:

- invalid behavior is rejected before execution;
- finite retry/replan fixtures remain accepted;
- fallback cannot silently broaden effect, authority, egress, or semantic port contract.

### IR7 — Optimization non-escalation

Attempt optimizer transformations that:

- remove numeric verification;
- change egress from deny to policy;
- add an authority request;
- change a deterministic node to probabilistic.

Pass criteria:

- transformed program is considered semantically different and cannot replace the validated program silently;
- any broader authority/egress requires normal policy evaluation and, where applicable, approval.

### IR8 — Capability/provider conformance boundary

Register two providers for one semantic capability contract and one intentionally non-conforming provider.

Pass criteria:

- both conforming providers can satisfy the same IR node without modifying AIOS IR;
- non-conforming provider is rejected before execution;
- provider effect/authority/runtime claims are within semantic-contract envelopes;
- provider choice appears in Execution Binding/provenance, not as a mutation of semantic IR.

## Demonstration A — Task-first data analysis

User intent:

> Understand these numbers, identify important changes, create a chart and a concise report, and save the result.

### A1 — Task creation

**Given** a CSV or XLSX input,
**when** the user submits the intent,
**then** the system creates a stable Task ID and records the original intent.

Pass criteria:

- Task ID exists;
- input is referenced by an Artifact/data handle;
- original user text is retained;
- no spreadsheet application is manually launched by the user.

### A2 — Typed planning and IR normalization

The planner must produce a typed proposal whose executable meaning can be normalized into valid AIOS IR.

Pass criteria:

- planner proposal validates against the planner/task-plan contract as applicable;
- normalized executable graph validates against AIOS IR structural and semantic rules;
- unknown capabilities fail closed;
- malformed provider/model output cannot enter execution directly;
- no planner output itself grants authority.

### A3 — Least authority

An Issue #3 candidate reservation is non-executable before policy/grant issuance: no Execution Binding, step attempt, or grant exists merely because its IDs and exact resources are reserved. Replaying an identical PENDING reservation is idempotent; changing Task, program, provider, action, Artifact, allocation identity, or binding/attempt ID fails closed. Crash recovery must preserve a partial proposal only as non-authority, and stamped reservation guard replacement must fail store preflight.

The Task receives read authority to the selected input and write authority only to bound Task output allocations/resources.

Pass criteria:

- attempting to read an unrelated protected test Artifact/file is denied;
- attempting to write outside the allowed output allocation is denied;
- denial is represented in provenance;
- planner/provider cannot broaden its own grant;
- point-of-use grant validation rejects expired/revoked/wrong-Task/wrong-provider/wrong-binding authority.

### A4 — Provider substitution

At least one capability must have two conforming providers.

Pass criteria:

- changing policy/provider priority switches the selected provider;
- AIOS IR/Task semantics remain unchanged;
- semantic program hash remains unchanged;
- replacement creates a new Execution Binding/attempt where execution has begun;
- resulting provider/build choice is visible in execution/provenance.

### A5 — Deterministic calculations

Numerical calculations used in the report must be produced or independently checked by a deterministic provider.

Pass criteria:

- fixture dataset has known expected totals/statistics;
- verifier detects an intentionally injected incorrect model claim;
- incorrect values do not silently become final report facts;
- compiled/reused workflows do not remove the verifier.

### A6 — Artifact production

The Task must produce at least:

- one chart image or vector Artifact;
- one concise report Artifact;
- one machine-readable analysis Artifact containing key computed values.

Pass criteria:

- Artifacts are ordinary portable formats;
- each Artifact has a stable handle and content hash when practical;
- lineage links outputs to input and producing steps;
- staging bytes do not become visible output before trusted publication/finalization;
- committed publication is idempotent under response loss.

### A7 — Provenance inspection

The user/developer can inspect:

- Task creation;
- plan/IR revisions and semantic program hash;
- Registry Snapshot used for validation;
- capability/provider/build selections;
- policy decisions/approvals/grants;
- Execution Bindings/attempts;
- model calls;
- external data transfers, if any;
- produced Artifacts;
- verification;
- crash/recovery/reconciliation where applicable;
- completion/failure.

### A8 — Local/remote policy routing

If a remote provider is configured, policy can force the same eligible reasoning step local or remote.

Pass criteria:

- local-only mode causes no remote network transfer for protected Task data;
- network connectivity alone does not authorize Task data egress;
- remote mode records provider/service/destination identity and egress metadata;
- absence of a remote provider does not prevent deterministic Task execution when local capabilities are sufficient;
- eligible local/remote provider changes do not alter the semantic program solely because placement changed;
- credential exportability/locality is not silently weakened by remote placement.

### A9 — Planner failure containment

Inject planner failure cases:

- invalid JSON/typed output;
- unknown capability;
- request for unauthorized path/resource;
- attempt to invoke shell arbitrarily;
- contradictory steps;
- excessive/repeated planning;
- attempt to embed provider/grant/secret/runtime state in semantic IR.

Pass criteria:

- no case bypasses IR validation or deterministic authorization;
- no generic shell route executes model-generated command strings as trusted provider invocation;
- Task transitions to recoverable failure/clarification state rather than corrupting host state.

### A10 — Reusable procedure

Execute the same Task pattern repeatedly with structurally similar inputs.

Pass criteria:

- system can identify a reusable semantic graph pattern;
- a versioned Skill/procedure Artifact can be created explicitly;
- Skill records source IR hash/template and validation evidence;
- subsequent execution can reuse/compile deterministic portions;
- saved Skill still undergoes current policy/provider validation;
- Skill carries no old capability grants/approval tokens/credential material;
- new input data produces new derived outputs rather than stale reused values;
- model/planning work reduction is measurable.

## Demonstration B — Legacy application routing

Initial practical target: one Windows application that is legally redistributable or user-supplied and works through a mature Windows-on-Linux compatibility environment.

### B1 — Package inspection

Pass criteria:

- broker identifies package/binary family and CPU architecture;
- unsupported/unknown types fail safely;
- inspection does not execute the package.

### B2 — Habitat selection

Pass criteria:

- broker chooses a named Windows compatibility habitat;
- the choice and compatibility profile are inspectable;
- user is not required to manually configure the compatibility layer for the known fixture.

### B3 — Sandboxed launch

Pass criteria:

- application launches successfully;
- filesystem access is limited to assigned test resources/projections;
- network policy is applied;
- host secrets/credentials are unavailable unless explicitly mediated/granted.

### B4 — Failure mode

Provide an intentionally incompatible or malformed fixture.

Pass criteria:

- failure is reported as a compatibility result rather than a generic OS crash;
- host remains intact;
- relevant diagnostics are retained.

## Hardware/resource tests

### H1 — Hardware profile

Pass criteria:

- CPU architecture, logical cores, RAM, storage availability, and detectable accelerator metadata are represented in a structured hardware profile;
- missing accelerator support does not make the profile invalid.

### H2 — Constrained profile simulation

Run with artificial resource limits.

Pass criteria:

- broker changes provider/placement selection or reports a bounded inability;
- Task identity, semantic IR, and security policy remain unchanged unless a genuine semantic replan is necessary.

## Recovery tests

The detailed trusted-core crash matrix is in `docs/86-stage1-deterministic-spine-acceptance-tests.md`.

### R1 — Provider crash

Kill a provider during Task execution.

Pass criteria:

- Task records failure/uncertainty;
- broker may retry/select another conforming provider only according to bounded IR failure policy + recovery certainty/current policy;
- completed prior Artifacts are not silently misrepresented as complete Task output;
- replacement execution uses a new attempt/Execution Binding.

### R2 — Control-plane restart

Restart the prototype control-plane process during a persisted Task.

Pass criteria:

- Task state/provenance recover from durable storage;
- semantic program hash/Registry Snapshot/historical bindings remain interpretable;
- expired/revoked authority is not resurrected;
- Task enters deterministic recoverable/paused/failed/reconciliation state as appropriate;
- purely local recovery does not require an AI model.

### R3 — Response-loss/idempotency

Simulate committed Task transition/Artifact publication/grant consumption followed by caller-response loss.

Pass criteria:

- repeated stable request IDs return/reconcile committed outcome;
- state transition/publication/grant use is not applied twice;
- no provider rerun solely because the caller missed a response.

### R4 — Unknown external outcome

Use a synthetic opaque external operation where request may have reached the external system but response is lost and no authoritative status/idempotency proof is available.

Pass criteria:

- outcome is `OUTCOME_UNKNOWN`;
- automatic blind retry is blocked;
- reconciliation/human-safe path is required;
- model assertion cannot change the certainty classification.

## Security regression fixtures

The v0.1 test suite should include at least these adversarial fixtures:

1. prompt injection embedded in a CSV cell;
2. prompt injection embedded in document metadata;
3. provider requests unrelated filesystem/Artifact access;
4. provider attempts network egress in local-only mode;
5. model returns executable shell text instead of required schema;
6. legacy application attempts to read a host-only test secret;
7. stale/expired/revoked capability grant/token handle;
8. capability grant for wrong Task/provider/binding/resource;
9. Artifact hash mismatch;
10. malicious/invalid provider manifest;
11. provider claims semantic capability but violates ports/effects/authority;
12. IR attempts to embed provider identity or grant/token;
13. compiled Skill attempts to reuse previous Task authority/credentials;
14. optimizer removes verification node or broadens egress/effects;
15. type mismatch between connected capability ports;
16. raw long-lived credential appears in provider invocation/provenance/log path;
17. local-restricted credential follows remote placement;
18. staging Artifact bytes appear as published output after crash, or denied/unreceipted pending bytes are promoted to the canonical blob namespace merely because their filename and hash match;
19. response loss causes duplicate Task transition/publication/provider execution;
20. `OUTCOME_UNKNOWN` operation is blindly retried;
21. provider result exit code 0 with malformed structured result is treated as success;
22. provider substitution mutates prior Execution Binding instead of creating new history;
23. network-connect grant is incorrectly accepted as data-egress authority;
24. stale approval remains valid after destination/provider/semantic-program change.
25. an export adapter's writer or factory capture performs an unadmitted
    destructor effect after success, denial, revocation, or handle drop.

## v0.1 release gate

v0.1 is not complete until:

- Issue #17 validator/IR gate passes;
- Stage-1 deterministic spine gate (`docs/86`) passes;
- IR1–IR8 pass;
- Demonstration A passes A1–A10;
- Demonstration B passes B1–B4 for one known fixture;
- H1–H2 and R1–R4 pass;
- all security regression fixtures fail safely;
- a fresh developer can reproduce the demo from repository instructions;
- architecture documentation is updated to match implemented contracts rather than describing an imaginary future system.
